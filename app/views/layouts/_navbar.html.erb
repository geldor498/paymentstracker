<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Tracker</a>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto ">
      <li class="nav-item  <% if current_page?(:controller => 'reports') %> active <% end %>">
        <%= link_to 'Reports', reports_index_path, {class: "nav-link"}%>
      </li>
      <li class="nav-item <% if current_page?(:controller => 'categories') %>active <% end %>">
        <%= link_to 'Categories', categories_path, {class: "nav-link"}%>
      </li>
      <li class="nav-item <% if current_page?(:controller => 'payments') %>active <% end %>">
        <%= link_to 'Payments', payments_path, {class: "nav-link"}%>
      </li>
    </ul>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to('Logout', destroy_user_session_path, {class: "nav-link", method: :delete}) %>        
        </li>
        <% else %>
        <li class="nav-item">
          <%= link_to('Login', new_user_session_path)  %>  
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
